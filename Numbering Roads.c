#include<stdio.h>

int main (void)
{
    int R, N, D, i;
    for(i = 1; scanf("%d%d", &R, &N) && R ;)
    {
        printf();

        printf((R-1)/N>26 ? "Case %d: impossible\n", i++ : "Case %d: %d\n", i++, D);

    }
    return 0;
}

